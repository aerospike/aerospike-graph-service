package com.aerospike.firefly.structure;

// This is a dummy class that allows us to instantiate a logger in FireflyGraph.java that says AerospikeGraphService.
// We can eventually migrate to calling FireflyGraph AerospikeGraphService but this requires docs changes, config
// updates, etc, and isn't worth it right now.
public class AerospikeGraphService {
}
