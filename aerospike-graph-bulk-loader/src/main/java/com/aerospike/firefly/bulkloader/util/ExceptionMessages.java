package com.aerospike.firefly.bulkloader.util;

public class ExceptionMessages {
    public static final String DATABASE_NOT_EMPTY = "Can not bulk load into a database that contains existing Vertices or Edges.";
    public static final String JOB_ALREADY_RUNNING = "A bulk load is already currently in progress.";
}
